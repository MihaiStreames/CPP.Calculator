#ifndef CALCULATOR_SQRT_H
#define CALCULATOR_SQRT_H

#include "../calculator.h"

namespace Operations {
    double SquareRoot(double n, bool& errorFlag);
    void Sqrt(Calc& calc);
}

#endif //CALCULATOR_SQRT_H